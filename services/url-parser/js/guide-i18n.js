window.GUIDES = {
"ru": "<h2>Один URL под микроскопом</h2><p>Возьмём пример из поля ввода: <code>https://shop.example.com:8080/products/shoes?color=red&amp;size=10&amp;color=blue#reviews</code>. Браузер видит не одну длинную строку. Он видит семь полей, и каждое из них меняет поведение запроса.</p><p>Схема — <code>https:</code>, обратите внимание на двоеточие: стандарт URL считает его частью свойства protocol. Хост — <code>shop.example.com</code>, порт — <code>8080</code>, он указан явно, потому что не совпадает с портом 443, принятым для HTTPS по умолчанию. Вместе со схемой эти три части образуют источник (origin), <code>https://shop.example.com:8080</code>, а источник — это та единица, которой браузеры оперируют в политике одного источника, в привязке cookie к хосту и в решениях CORS. Смените порт — и получите другой источник, даже на той же машине.</p><p>Путь — <code>/products/shoes</code>, он делится на два сегмента: <code>products</code> и <code>shoes</code>. Сегменты важны, потому что маршрутизаторы сопоставляют их по позиции: лишний сегмент в конце или случайная двойная косая черта незаметно дают 404, неотличимый от опечатки. Далее строка запроса несёт три пары, а фрагмент — <code>reviews</code>.</p><h3>Зачем нужен переключатель кодирования</h3><p>URL способен передавать лишь ограниченный набор ASCII-символов, и часть из них зарезервирована, поскольку служит разделителями. Всё остальное путешествует с помощью процентного кодирования: байт записывается как знак процента и две шестнадцатеричные цифры. Пробел превращается в <code>%20</code> (или в плюс внутри строки запроса), амперсанд — в <code>%26</code>, вопросительный знак — в <code>%3F</code>, косая черта внутри значения — в <code>%2F</code>, а не-ASCII текст сначала переводится в UTF-8, поэтому é становится <code>%C3%A9</code>, а эмодзи — четырьмя процентными группами.</p><p>Практическое следствие: <code>?q=fish%20%26%20chips</code> — это один параметр со значением «fish &amp; chips», тогда как <code>?q=fish &amp; chips</code> — три сломанные сущности. Включайте необработанные значения, когда сравниваете то, что клиент действительно отправил, с тем, что записал сервер; выключайте, когда просто хотите прочитать данные. Частая ошибка — двойное кодирование, когда появляется <code>%2520</code>, потому что значение, уже содержавшее <code>%20</code>, закодировали второй раз. В декодированном виде это читается как <code>%20</code>, а не как пробел, — вот и признак.</p><h3>Ловушка повторяющихся параметров</h3><p>В URL из примера <code>color</code> встречается дважды. Этот разборщик отказывается их объединять и показывает обе строки по порядку, потому что правильный ответ действительно зависит от принимающего стека. PHP оставляет последнее значение, если только ключ не записан как <code>color[]</code>. Express и Rails отдают массив. ASP.NET склеивает значения запятой, получая строку «red,blue». Java-сервлеты предоставляют getParameterValues, но getParameter возвращает только первое. Поэтому ссылка, работающая на staging, в продакшене может тихо выбрать не тот цвет. Если вы формируете URL программно, убирайте дубликаты до сборки строки запроса, а не после.</p><h2>Повседневные сценарии отладки</h2><h3>Аудит параметров отслеживания</h3><p>Вставьте ссылку рекламной кампании и посмотрите таблицу параметров. Обычно там найдутся <code>utm_source</code>, <code>utm_medium</code>, <code>utm_campaign</code>, а также идентификаторы площадок вроде <code>gclid</code>, <code>fbclid</code> или <code>msclkid</code>. Их добавляют рекламные платформы, и для вашей канонической страницы они невидимы, но поисковые системы считают их отдельными URL, пока тег canonical не скажет обратное. Увидеть весь список сразу — самый быстрый способ решить, какие из них отбросить в настройках аналитики.</p><h3>Исправление canonical и редиректов</h3><p>Сравните разобранные источник и путь URL, о котором сообщил краулер, с той канонической ссылкой, которую вы задумывали. Расхождения почти всегда приходят из одного из четырёх мест: схема http вместо https, хост с www вместо корневого домена, разница в завершающей косой черте или сегмент в верхнем регистре. Для краулера все четыре — разные URL, даже если отображается одна и та же страница.</p><h3>Проверка того, что вообще видит сервер</h3><p>Фрагмент после решётки никогда не покидает браузер. Если в тикете поддержки написано «ссылка с #step=3 не работает», в логах сервера будут только путь и запрос, потому что браузер удалил всё начиная с решётки до отправки запроса. Всё, на что должен реагировать бэкенд, место в пути или в строке запроса.</p><h2>Ограничения, о которых стоит знать</h2><p>Инструмент использует тот же разборщик WHATWG, что и браузер, поэтому его результаты совпадают с тем, что увидит ваш JavaScript, включая особенности вроде приведения имён хостов к нижнему регистру, преобразования интернациональных доменов в punycode и отбрасывания порта по умолчанию в источнике. Он не загружает URL, поэтому не скажет, существует ли страница, есть ли редирект и действителен ли сертификат. Очень длинные строки запроса здесь разбираются нормально, но серверы и прокси обычно ограничивают строку запроса 4–8 КБ, так что безупречно разобранный URL всё равно может быть отвергнут по пути. А если схема отсутствует, инструмент предполагает https и сообщает об этом над результатами — удобно при вставке голого домена, но не воспринимайте это допущение как утверждение, что сайт поддерживает TLS.</p>",
"pt": "<h2>Uma URL, dissecada</h2><p>Veja o exemplo na caixa: <code>https://shop.example.com:8080/products/shoes?color=red&amp;size=10&amp;color=blue#reviews</code>. O navegador não enxerga uma longa cadeia de texto. Ele enxerga sete campos, e cada um deles muda o comportamento de uma requisição.</p><p>O esquema é <code>https:</code> — repare nos dois-pontos, que o padrão de URL trata como parte da propriedade protocol. O host é <code>shop.example.com</code> e a porta é <code>8080</code>, explícita aqui porque não é a porta padrão 443 do HTTPS. Junto com o esquema, esses três formam a origem, <code>https://shop.example.com:8080</code>, e a origem é a unidade que os navegadores usam para a política de mesma origem, para cookies delimitados por host e para decisões de CORS. Mude a porta e você terá outra origem, mesmo na mesma máquina.</p><p>O caminho é <code>/products/shoes</code>, que se divide em dois segmentos: <code>products</code> e <code>shoes</code>. Segmentos importam porque os roteadores os comparam por posição — um segmento final a mais ou uma barra dupla perdida produz silenciosamente um 404 idêntico ao de um erro de digitação. Em seguida, a query string carrega três pares, e o fragmento é <code>reviews</code>.</p><h3>Por que existe a opção de codificação</h3><p>Uma URL só consegue transportar um alfabeto ASCII limitado, e alguns desses caracteres são reservados porque funcionam como delimitadores. A codificação por porcentagem é como todo o resto viaja: o byte é escrito como um sinal de porcentagem mais dois dígitos hexadecimais. Um espaço vira <code>%20</code> (ou um sinal de mais dentro de uma query string), um E comercial vira <code>%26</code>, um ponto de interrogação vira <code>%3F</code>, uma barra dentro de um valor vira <code>%2F</code>, e o texto não ASCII é convertido antes para UTF-8, de modo que um é acentuado vira <code>%C3%A9</code> e um emoji vira quatro grupos de porcentagem.</p><p>A consequência prática: <code>?q=fish%20%26%20chips</code> é um único parâmetro cujo valor é \"fish &amp; chips\", enquanto <code>?q=fish &amp; chips</code> são três coisas quebradas. Ative os valores brutos quando estiver comparando o que um cliente realmente enviou com o que um servidor registrou; deixe desligado quando quiser apenas ler os dados. Um bug comum é a codificação dupla, em que aparece <code>%2520</code> porque um valor que já continha <code>%20</code> foi codificado uma segunda vez. Na visualização decodificada isso aparece como <code>%20</code> em vez de um espaço, e esse é o sinal.</p><h3>A armadilha dos parâmetros duplicados</h3><p>A URL de exemplo contém <code>color</code> duas vezes. Este analisador se recusa a mesclá-los e mostra as duas linhas em ordem, porque a resposta correta realmente depende da pilha que recebe a requisição. O PHP mantém o último valor, a menos que a chave seja escrita como <code>color[]</code>. Express e Rails entregam um array. O ASP.NET junta os valores com vírgula, produzindo a string \"red,blue\". Servlets Java expõem getParameterValues, mas getParameter retorna apenas o primeiro. Assim, um link que funciona em staging pode escolher silenciosamente a cor errada em produção. Se você gera URLs programaticamente, remova as duplicatas antes de montar a query string, não depois.</p><h2>Fluxos de depuração do dia a dia</h2><h3>Auditar parâmetros de rastreamento</h3><p>Cole um link de campanha e olhe a tabela de parâmetros. Normalmente você encontrará <code>utm_source</code>, <code>utm_medium</code>, <code>utm_campaign</code>, além de IDs de plataforma como <code>gclid</code>, <code>fbclid</code> ou <code>msclkid</code>. Eles são acrescentados pelas plataformas de anúncios e são invisíveis para a sua página canônica, mas os buscadores os enxergam como URLs distintas, a menos que uma tag canonical diga o contrário. Ver a lista completa de uma vez é a maneira mais rápida de decidir quais remover na configuração da sua analytics.</p><h3>Corrigir canônicas e redirecionamentos</h3><p>Compare a origem e o caminho analisados da URL que um rastreador reportou com a canônica que você pretendia. As divergências quase sempre vêm de um de quatro pontos: esquema http em vez de https, host com www em vez do domínio raiz, diferença na barra final ou um segmento em maiúsculas. Para um rastreador, os quatro casos são URLs diferentes, mesmo que exibam a mesma página.</p><h3>Verificar o que o servidor consegue ver</h3><p>O fragmento após a cerquilha nunca sai do navegador. Se um chamado de suporte diz \"o link com #step=3 não funciona\", os logs do servidor mostrarão apenas o caminho e a query, porque o navegador removeu tudo a partir da cerquilha antes de enviar a requisição. Qualquer coisa a que o backend precise reagir pertence ao caminho ou à query string.</p><h2>Limites que vale conhecer</h2><p>Esta ferramenta usa o mesmo analisador WHATWG que o navegador usa, então seus resultados coincidem com o que o seu JavaScript verá — incluindo peculiaridades como nomes de host em minúsculas, conversão para punycode de domínios internacionais e a omissão da porta padrão na origem. Ela não busca a URL, portanto não pode dizer se a página existe, se redireciona ou se o certificado é válido. Query strings muito longas são analisadas sem problema aqui, mas servidores e proxies costumam limitar a linha de requisição a 4 a 8 KB, de modo que uma URL que é analisada corretamente ainda pode ser rejeitada em trânsito. E quando falta o esquema, a ferramenta assume https e avisa acima dos resultados — útil para colar um domínio puro, mas não interprete essa suposição como uma afirmação de que o site oferece suporte a TLS.</p>",
"de": "<h2>Eine URL, seziert</h2><p>Nehmen wir das Beispiel im Eingabefeld: <code>https://shop.example.com:8080/products/shoes?color=red&amp;size=10&amp;color=blue#reviews</code>. Der Browser sieht darin keine lange Zeichenkette. Er sieht sieben Felder, und jedes davon verändert das Verhalten einer Anfrage.</p><p>Das Schema ist <code>https:</code> — man beachte den Doppelpunkt, den der URL-Standard als Teil der Eigenschaft protocol behandelt. Der Host ist <code>shop.example.com</code>, der Port ist <code>8080</code> und steht hier ausdrücklich da, weil er nicht dem HTTPS-Standardport 443 entspricht. Zusammen mit dem Schema bilden diese drei den Origin, <code>https://shop.example.com:8080</code>, und der Origin ist die Einheit, mit der Browser die Same-Origin-Policy, hostgebundene Cookies und CORS-Entscheidungen handhaben. Ändern Sie den Port, haben Sie einen anderen Origin — sogar auf demselben Rechner.</p><p>Der Pfad ist <code>/products/shoes</code> und zerfällt in zwei Segmente: <code>products</code> und <code>shoes</code>. Segmente sind wichtig, weil Router sie nach Position abgleichen — ein überzähliges Endsegment oder ein versehentlicher doppelter Schrägstrich erzeugt still und leise einen 404, der von einem Tippfehler nicht zu unterscheiden ist. Danach trägt der Query-String drei Paare, und das Fragment lautet <code>reviews</code>.</p><h3>Warum es den Kodierungsschalter gibt</h3><p>Eine URL kann nur ein begrenztes ASCII-Alphabet transportieren, und eine Handvoll dieser Zeichen ist reserviert, weil sie als Trennzeichen dienen. Prozentkodierung ist der Weg, auf dem alles Übrige reist: Das Byte wird als Prozentzeichen plus zwei Hexadezimalziffern geschrieben. Ein Leerzeichen wird zu <code>%20</code> (oder innerhalb eines Query-Strings zu einem Pluszeichen), ein Kaufmanns-Und zu <code>%26</code>, ein Fragezeichen zu <code>%3F</code>, ein Schrägstrich innerhalb eines Werts zu <code>%2F</code>, und Nicht-ASCII-Text wird zuerst in UTF-8 umgewandelt, sodass ein é zu <code>%C3%A9</code> wird und ein Emoji zu vier Prozentgruppen.</p><p>Die praktische Folge: <code>?q=fish%20%26%20chips</code> ist ein einziger Parameter mit dem Wert „fish &amp; chips“, während <code>?q=fish &amp; chips</code> drei kaputte Dinge sind. Schalten Sie die Rohwerte ein, wenn Sie vergleichen, was ein Client tatsächlich gesendet hat und was ein Server protokolliert hat; lassen Sie sie aus, wenn Sie die Daten nur lesen wollen. Ein häufiger Fehler ist die Doppelkodierung, bei der <code>%2520</code> auftaucht, weil ein Wert, der bereits <code>%20</code> enthielt, ein zweites Mal kodiert wurde. In der dekodierten Ansicht liest sich das als <code>%20</code> statt als Leerzeichen — genau daran erkennt man es.</p><h3>Die Falle doppelter Parameter</h3><p>Die Beispiel-URL enthält <code>color</code> zweimal. Dieser Parser weigert sich, sie zusammenzuführen, und zeigt beide Zeilen der Reihe nach an, denn die richtige Antwort hängt tatsächlich vom empfangenden Stack ab. PHP behält den letzten Wert, sofern der Schlüssel nicht <code>color[]</code> geschrieben ist. Express und Rails liefern ein Array. ASP.NET verbindet sie mit einem Komma und erzeugt die Zeichenkette „red,blue“. Java-Servlets bieten getParameterValues an, aber getParameter gibt nur den ersten Wert zurück. So kann ein Link, der im Staging funktioniert, in der Produktion stillschweigend die falsche Farbe wählen. Wenn Sie URLs programmatisch erzeugen, entfernen Sie Duplikate, bevor Sie den Query-String bauen, nicht danach.</p><h2>Alltägliche Debugging-Abläufe</h2><h3>Tracking-Parameter prüfen</h3><p>Fügen Sie einen Kampagnenlink ein und sehen Sie sich die Parametertabelle an. Meist finden Sie <code>utm_source</code>, <code>utm_medium</code>, <code>utm_campaign</code> sowie Plattform-IDs wie <code>gclid</code>, <code>fbclid</code> oder <code>msclkid</code>. Sie werden von Werbeplattformen angehängt und sind für Ihre kanonische Seite unsichtbar, doch Suchmaschinen sehen darin eigene URLs, solange kein Canonical-Tag etwas anderes sagt. Die vollständige Liste auf einen Blick zu sehen ist der schnellste Weg zu entscheiden, welche Sie in Ihrer Analytics-Konfiguration entfernen.</p><h3>Canonicals und Weiterleitungen korrigieren</h3><p>Vergleichen Sie den geparsten Origin und Pfad der vom Crawler gemeldeten URL mit dem Canonical, das Sie beabsichtigt hatten. Abweichungen stammen fast immer aus einer von vier Quellen: Schema http statt https, Host mit www statt Apex-Domain, ein Unterschied beim abschließenden Schrägstrich oder ein Segment in Großbuchstaben. Für einen Crawler sind alle vier eigenständige URLs, selbst wenn sie dieselbe Seite ausliefern.</p><h3>Prüfen, was der Server überhaupt sieht</h3><p>Das Fragment nach der Raute verlässt den Browser nie. Wenn ein Support-Ticket sagt „der Link mit #step=3 funktioniert nicht“, zeigen die Serverlogs nur Pfad und Query, weil der Browser alles ab der Raute vor dem Absenden entfernt hat. Alles, worauf das Backend reagieren muss, gehört in den Pfad oder den Query-String.</p><h2>Grenzen, die man kennen sollte</h2><p>Dieses Werkzeug nutzt denselben WHATWG-Parser wie der Browser, seine Ergebnisse entsprechen also dem, was Ihr JavaScript sehen wird — einschließlich Eigenheiten wie kleingeschriebenen Hostnamen, der Punycode-Umwandlung internationaler Domains und dem Wegfall des Standardports im Origin. Es ruft die URL nicht ab und kann Ihnen daher nicht sagen, ob die Seite existiert, ob sie weiterleitet oder ob das Zertifikat gültig ist. Sehr lange Query-Strings werden hier problemlos geparst, doch Server und Proxys begrenzen die Anfragezeile üblicherweise auf 4 bis 8 KB, sodass eine sauber geparste URL unterwegs dennoch abgelehnt werden kann. Und fehlt ein Schema, nimmt das Werkzeug https an und weist oberhalb der Ergebnisse darauf hin — praktisch, um eine nackte Domain einzufügen, aber verstehen Sie diese Annahme nicht als Aussage, dass die Website TLS unterstützt.</p>",
"fr": "<h2>Une URL, disséquée</h2><p>Prenons l'exemple affiché dans le champ : <code>https://shop.example.com:8080/products/shoes?color=red&amp;size=10&amp;color=blue#reviews</code>. Le navigateur n'y voit pas une longue chaîne. Il y voit sept champs, et chacun d'eux modifie le comportement d'une requête.</p><p>Le schéma est <code>https:</code> — remarquez les deux-points, que le standard URL considère comme faisant partie de la propriété protocol. L'hôte est <code>shop.example.com</code> et le port est <code>8080</code>, explicite ici parce qu'il ne s'agit pas du port 443 par défaut de HTTPS. Avec le schéma, ces trois éléments forment l'origine, <code>https://shop.example.com:8080</code>, et l'origine est l'unité que les navigateurs utilisent pour la politique de même origine, les cookies délimités par hôte et les décisions CORS. Changez le port et vous avez une autre origine, même sur la même machine.</p><p>Le chemin est <code>/products/shoes</code>, qui se découpe en deux segments : <code>products</code> et <code>shoes</code>. Les segments comptent parce que les routeurs les font correspondre par position — un segment final en trop ou une double barre oblique égarée produit discrètement un 404 impossible à distinguer d'une faute de frappe. Ensuite, la chaîne de requête transporte trois paires, et le fragment est <code>reviews</code>.</p><h3>Pourquoi l'option d'encodage existe</h3><p>Une URL ne peut transporter qu'un alphabet ASCII limité, et une poignée de ces caractères sont réservés parce qu'ils servent de délimiteurs. L'encodage-pourcent est la façon dont tout le reste circule : l'octet s'écrit sous la forme d'un signe pourcent suivi de deux chiffres hexadécimaux. Une espace devient <code>%20</code> (ou un signe plus dans une chaîne de requête), une esperluette devient <code>%26</code>, un point d'interrogation devient <code>%3F</code>, une barre oblique à l'intérieur d'une valeur devient <code>%2F</code>, et le texte non ASCII est d'abord converti en UTF-8, si bien qu'un e accent aigu devient <code>%C3%A9</code> et un emoji, quatre groupes pourcent.</p><p>Conséquence pratique : <code>?q=fish%20%26%20chips</code> est un seul paramètre dont la valeur est « fish &amp; chips », tandis que <code>?q=fish &amp; chips</code> représente trois choses cassées. Activez les valeurs brutes lorsque vous comparez ce qu'un client a réellement envoyé à ce qu'un serveur a journalisé ; laissez l'option désactivée quand vous voulez simplement lire les données. Un bug courant est le double encodage, où <code>%2520</code> apparaît parce qu'une valeur contenant déjà <code>%20</code> a été encodée une seconde fois. En vue décodée, cela se lit <code>%20</code> plutôt qu'une espace : c'est l'indice.</p><h3>Le piège des paramètres en double</h3><p>L'URL d'exemple contient deux fois <code>color</code>. Cet analyseur refuse de les fusionner et affiche les deux lignes dans l'ordre, car la bonne réponse dépend réellement de la pile qui reçoit la requête. PHP conserve la dernière valeur, sauf si la clé est écrite <code>color[]</code>. Express et Rails vous renvoient un tableau. ASP.NET les joint par une virgule, produisant la chaîne « red,blue ». Les servlets Java exposent getParameterValues, mais getParameter ne renvoie que la première. Ainsi, un lien qui fonctionne en préproduction peut silencieusement choisir la mauvaise couleur en production. Si vous générez des URL par programme, dédupliquez avant de construire la chaîne de requête, et non après.</p><h2>Flux de débogage au quotidien</h2><h3>Auditer les paramètres de tracking</h3><p>Collez un lien de campagne et regardez le tableau des paramètres. Vous y trouverez généralement <code>utm_source</code>, <code>utm_medium</code>, <code>utm_campaign</code>, ainsi que des identifiants de plateforme comme <code>gclid</code>, <code>fbclid</code> ou <code>msclkid</code>. Ils sont ajoutés par les régies publicitaires et sont invisibles pour votre page canonique, mais les moteurs de recherche les voient comme des URL distinctes tant qu'une balise canonical ne dit pas le contraire. Voir la liste complète d'un coup d'œil est le moyen le plus rapide de décider lesquels supprimer dans votre configuration analytique.</p><h3>Corriger les canoniques et les redirections</h3><p>Comparez l'origine et le chemin analysés de l'URL signalée par un robot d'exploration avec la canonique que vous visiez. Les écarts viennent presque toujours de l'un de ces quatre points : un schéma http au lieu de https, un hôte www au lieu du domaine racine, une différence de barre oblique finale, ou un segment en majuscules. Pour un robot, ces quatre cas sont des URL distinctes, même si la page affichée est identique.</p><h3>Vérifier ce que le serveur peut voir</h3><p>Le fragment qui suit le dièse ne quitte jamais le navigateur. Si un ticket de support indique « le lien avec #step=3 ne marche pas », les journaux du serveur ne montreront que le chemin et la requête, car le navigateur a supprimé tout ce qui suit le dièse avant d'envoyer la requête. Tout ce à quoi le backend doit réagir a sa place dans le chemin ou la chaîne de requête.</p><h2>Limites à connaître</h2><p>Cet outil utilise le même analyseur WHATWG que le navigateur : ses résultats correspondent donc à ce que verra votre JavaScript, y compris des particularités comme les noms d'hôte mis en minuscules, la conversion en punycode des domaines internationaux et le port par défaut retiré de l'origine. Il ne charge pas l'URL, il ne peut donc pas vous dire si la page existe, si elle redirige, ou si le certificat est valide. Les chaînes de requête très longues s'analysent bien ici, mais serveurs et proxys plafonnent couramment la ligne de requête à 4 à 8 Ko : une URL qui s'analyse proprement peut malgré tout être rejetée en transit. Et lorsqu'un schéma manque, l'outil suppose https et le signale au-dessus des résultats — pratique pour coller un simple nom de domaine, mais ne prenez pas cette hypothèse pour une affirmation que le site prend en charge TLS.</p>",
"es": "<h2>Una URL, diseccionada</h2><p>Toma el ejemplo del cuadro: <code>https://shop.example.com:8080/products/shoes?color=red&amp;size=10&amp;color=blue#reviews</code>. El navegador no ve una cadena larga: ve siete campos, y cada uno de ellos cambia el comportamiento de la petición.</p><p>El esquema es <code>https:</code> —fíjate en los dos puntos, que el estándar de URL considera parte de la propiedad protocol—. El host es <code>shop.example.com</code> y el puerto es <code>8080</code>, explícito aquí porque no es el 443 predeterminado de HTTPS. Junto con el esquema, esos tres forman el origen, <code>https://shop.example.com:8080</code>, y el origen es la unidad que los navegadores usan para la política del mismo origen, las cookies delimitadas por host y las decisiones de CORS. Cambia el puerto y tendrás un origen distinto, incluso en la misma máquina.</p><p>La ruta es <code>/products/shoes</code>, que se divide en dos segmentos: <code>products</code> y <code>shoes</code>. Los segmentos importan porque los enrutadores los comparan por posición: un segmento final de más o una barra doble perdida produce en silencio un 404 idéntico al de una errata. Después, la cadena de consulta lleva tres pares y el fragmento es <code>reviews</code>.</p><h3>Por qué existe el interruptor de codificación</h3><p>Una URL solo puede transportar un alfabeto ASCII limitado, y unos cuantos de esos caracteres están reservados porque actúan como delimitadores. La codificación porcentual es la forma en que viaja todo lo demás: el byte se escribe como un signo de porcentaje más dos dígitos hexadecimales. Un espacio se convierte en <code>%20</code> (o en un signo más dentro de una cadena de consulta), un ampersand en <code>%26</code>, un signo de interrogación en <code>%3F</code>, una barra dentro de un valor en <code>%2F</code>, y el texto no ASCII se convierte antes a UTF-8, así que una e con acento se vuelve <code>%C3%A9</code> y un emoji, cuatro grupos porcentuales.</p><p>La consecuencia práctica: <code>?q=fish%20%26%20chips</code> es un único parámetro cuyo valor es «fish &amp; chips», mientras que <code>?q=fish &amp; chips</code> son tres cosas rotas. Activa los valores en crudo cuando compares lo que un cliente envió realmente con lo que registró un servidor; déjalo desactivado cuando solo quieras leer los datos. Un fallo habitual es la doble codificación, donde aparece <code>%2520</code> porque un valor que ya contenía <code>%20</code> se codificó una segunda vez. En la vista decodificada eso se lee como <code>%20</code> en vez de un espacio, y esa es la pista.</p><h3>La trampa de los parámetros duplicados</h3><p>La URL de ejemplo contiene <code>color</code> dos veces. Este analizador se niega a fusionarlos y muestra ambas filas en orden, porque la respuesta correcta depende de verdad de la pila que recibe la petición. PHP conserva el último valor salvo que la clave se escriba <code>color[]</code>. Express y Rails te entregan un array. ASP.NET los une con una coma y produce la cadena «red,blue». Los servlets de Java exponen getParameterValues, pero getParameter devuelve solo el primero. Así, un enlace que funciona en staging puede elegir en silencio el color equivocado en producción. Si generas URL por código, elimina duplicados antes de construir la cadena de consulta, no después.</p><h2>Flujos de depuración del día a día</h2><h3>Auditar parámetros de seguimiento</h3><p>Pega un enlace de campaña y mira la tabla de parámetros. Normalmente encontrarás <code>utm_source</code>, <code>utm_medium</code>, <code>utm_campaign</code>, además de identificadores de plataforma como <code>gclid</code>, <code>fbclid</code> o <code>msclkid</code>. Los añaden las plataformas publicitarias y son invisibles para tu página canónica, pero los buscadores los ven como URL distintas salvo que una etiqueta canonical diga lo contrario. Ver la lista completa de golpe es la manera más rápida de decidir cuáles descartar en la configuración de analítica.</p><h3>Arreglar canónicas y redirecciones</h3><p>Compara el origen y la ruta analizados de la URL que reportó un rastreador con la canónica que pretendías. Los desajustes casi siempre vienen de uno de cuatro sitios: esquema http frente a https, host con www frente al dominio raíz, diferencia en la barra final o un segmento en mayúsculas. Para un rastreador, los cuatro son URL distintas aunque muestren la misma página.</p><h3>Comprobar qué puede ver siquiera el servidor</h3><p>El fragmento que sigue a la almohadilla nunca sale del navegador. Si un ticket de soporte dice «el enlace con #step=3 no funciona», los registros del servidor mostrarán solo la ruta y la consulta, porque el navegador eliminó todo lo que había desde la almohadilla antes de enviar la petición. Cualquier cosa a la que el backend deba reaccionar va en la ruta o en la cadena de consulta.</p><h2>Límites que conviene conocer</h2><p>Esta herramienta usa el mismo analizador WHATWG que el navegador, así que sus resultados coinciden con lo que verá tu JavaScript, incluidas rarezas como los nombres de host en minúsculas, la conversión a punycode de los dominios internacionales y la omisión del puerto predeterminado en el origen. No descarga la URL, así que no puede decirte si la página existe, si redirige o si el certificado es válido. Las cadenas de consulta muy largas se analizan bien aquí, pero servidores y proxies suelen limitar la línea de petición a entre 4 y 8 KB, de modo que una URL que se analiza sin problema puede ser rechazada en tránsito. Y cuando falta el esquema, la herramienta asume https y lo indica encima de los resultados: útil para pegar un dominio a secas, pero no interpretes esa suposición como una afirmación de que el sitio admite TLS.</p>",
"zh": "<h2>解剖一条 URL</h2><p>看输入框里的示例：<code>https://shop.example.com:8080/products/shoes?color=red&amp;size=10&amp;color=blue#reviews</code>。浏览器并不把它看作一串长文本，而是看作七个字段，每一个都会改变请求的行为。</p><p>协议是 <code>https:</code>——注意冒号，URL 标准把冒号视为 protocol 属性的一部分。主机是 <code>shop.example.com</code>，端口是 <code>8080</code>，之所以显式写出，是因为它不是 HTTPS 的默认端口 443。这三者共同构成源 <code>https://shop.example.com:8080</code>，而源正是浏览器判断同源策略、按主机划分的 Cookie 以及 CORS 时使用的单位。哪怕在同一台机器上，换个端口就是另一个源。</p><p>路径是 <code>/products/shoes</code>，拆成两个片段：<code>products</code> 和 <code>shoes</code>。片段之所以重要，是因为路由按位置匹配它们——末尾多出一个片段或多写一个斜杠，就会悄悄产生一个与拼写错误毫无区别的 404。接着查询字符串携带三组键值，片段标识是 <code>reviews</code>。</p><h3>为什么要有编码开关</h3><p>URL 只能承载有限的 ASCII 字符，其中少数字符因为充当分隔符而被保留。其余内容一律通过百分号编码传输：把字节写成百分号加两位十六进制。空格变成 <code>%20</code>（在查询字符串里也可写作加号），&amp; 变成 <code>%26</code>，问号变成 <code>%3F</code>，值内部的斜杠变成 <code>%2F</code>；非 ASCII 文本会先转成 UTF-8，所以 é 变成 <code>%C3%A9</code>，一个表情符号会变成四组百分号编码。</p><p>实际后果是：<code>?q=fish%20%26%20chips</code> 是一个参数，值为 “fish &amp; chips”；而 <code>?q=fish &amp; chips</code> 则是三段被破坏的内容。当你要比对客户端实际发送的内容与服务器记录的内容时，打开原始值显示；只想读数据时就关掉。一个常见 bug 是双重编码：本已包含 <code>%20</code> 的值又被编码一次，于是出现 <code>%2520</code>。在解码视图里它显示为 <code>%20</code> 而不是空格，这就是线索。</p><h3>重复参数的陷阱</h3><p>示例 URL 里 <code>color</code> 出现了两次。本解析器拒绝合并它们，而是按顺序显示两行，因为正确答案确实取决于接收端的技术栈。PHP 只保留最后一个值，除非键写成 <code>color[]</code>。Express 和 Rails 会给你一个数组。ASP.NET 用逗号拼接，得到字符串 “red,blue”。Java Servlet 提供 getParameterValues，但 getParameter 只返回第一个。于是在预发环境正常的链接，到生产环境可能悄悄选错颜色。如果你用程序生成 URL，请在拼接查询字符串之前去重，而不是之后。</p><h2>日常调试流程</h2><h3>审查跟踪参数</h3><p>粘贴一条投放链接，看参数表。通常会看到 <code>utm_source</code>、<code>utm_medium</code>、<code>utm_campaign</code>，以及 <code>gclid</code>、<code>fbclid</code> 或 <code>msclkid</code> 之类的平台 ID。它们由广告平台追加，对你的规范页面不可见，但除非有 canonical 标签声明，搜索引擎会把它们当作不同的 URL。一次看到完整清单，是决定在分析配置里剔除哪些参数的最快方式。</p><h3>修正 canonical 与重定向</h3><p>把爬虫报告的那条 URL 解析出的源与路径，跟你设定的 canonical 作对比。不一致几乎总是来自四处之一：http 与 https 的协议差异、www 与裸域主机差异、末尾斜杠的有无、含大写字母的片段。即使渲染的是同一个页面，对爬虫来说这四者都是不同的 URL。</p><h3>确认服务器究竟能看到什么</h3><p>井号之后的片段标识永远不会离开浏览器。如果工单说“带 #step=3 的链接打不开”，服务器日志里只会有路径和查询，因为浏览器在发送请求前就删掉了从井号开始的全部内容。后端必须据以响应的信息，应该放在路径或查询字符串里。</p><h2>值得了解的局限</h2><p>本工具使用与浏览器相同的 WHATWG 解析器，所以结果与你的 JavaScript 看到的一致，包括主机名被转为小写、国际化域名转成 punycode、默认端口从源中被省略等特性。它不会请求该 URL，因此无法告诉你页面是否存在、是否重定向、证书是否有效。很长的查询字符串在这里能正常解析，但服务器和代理通常把请求行限制在 4 到 8 KB，所以解析干净的 URL 仍可能在传输途中被拒。若缺少协议，工具会假定 https 并在结果上方说明——粘贴裸域名时很方便，但不要把这个假定当作该站点支持 TLS 的结论。</p>",
"ja": "<h2>1本のURLを解剖する</h2><p>入力欄の例を見てほしい。<code>https://shop.example.com:8080/products/shoes?color=red&amp;size=10&amp;color=blue#reviews</code>。ブラウザはこれを1本の長い文字列としては見ていない。7つのフィールドとして見ており、そのどれもがリクエストの挙動を左右する。</p><p>スキームは <code>https:</code> ——コロンまで含む。URL標準がコロンをプロトコルプロパティの一部として扱うからだ。ホストは <code>shop.example.com</code>、ポートは <code>8080</code> で、HTTPSの既定値である443ではないため明示されている。スキームと合わせてこの3つがオリジン <code>https://shop.example.com:8080</code> を構成し、オリジンはブラウザが同一オリジンポリシー、ホスト単位のCookie、CORSの判断に用いる単位である。同じマシン上でもポートが変われば別のオリジンになる。</p><p>パスは <code>/products/shoes</code> で、<code>products</code> と <code>shoes</code> の2つのセグメントに分かれる。ルーターはセグメントを位置で照合するためセグメントは重要だ——末尾に余分なセグメントが付いたり、スラッシュが二重になったりすると、打ち間違いと見分けのつかない404が静かに発生する。続くクエリ文字列は3組を運び、フラグメントは <code>reviews</code> である。</p><h3>エンコード切り替えがある理由</h3><p>URLが運べるのは限られたASCII文字だけで、そのうちいくつかは区切り記号として働くため予約されている。それ以外はすべてパーセントエンコーディングで運ばれる。バイトをパーセント記号と16進2桁で表す方式だ。空白は <code>%20</code>(クエリ文字列の中ではプラス記号)、アンパサンドは <code>%26</code>、疑問符は <code>%3F</code>、値の中のスラッシュは <code>%2F</code> になり、非ASCII文字はまずUTF-8に変換されるので é は <code>%C3%A9</code>、絵文字は4つのパーセント組になる。</p><p>実務上の帰結はこうだ。<code>?q=fish%20%26%20chips</code> は値が「fish &amp; chips」であるパラメータ1つだが、<code>?q=fish &amp; chips</code> は壊れた3つの断片になる。クライアントが実際に送った内容とサーバーが記録した内容を突き合わせるときは生の値表示をオンにし、データを読みたいだけならオフのままでよい。よくあるバグは二重エンコードで、すでに <code>%20</code> を含む値をもう一度エンコードした結果 <code>%2520</code> が現れる。デコード表示で空白ではなく <code>%20</code> と見えるのがその手掛かりだ。</p><h3>重複パラメータという罠</h3><p>例のURLには <code>color</code> が2回含まれている。このパーサーは両者をまとめず、順序どおり2行とも表示する。正しい答えが受け取る側のスタックによって本当に変わるからだ。PHPはキーが <code>color[]</code> と書かれていない限り最後の値を残す。ExpressとRailsは配列で渡す。ASP.NETはカンマで連結して「red,blue」という文字列にする。JavaサーブレットはgetParameterValuesを提供するが、getParameterは最初の値しか返さない。そのためステージングで動いていたリンクが本番では静かに違う色を選ぶことがある。URLをプログラムで生成するなら、クエリ文字列を組み立てた後ではなく組み立てる前に重複を除去すること。</p><h2>日常のデバッグ手順</h2><h3>トラッキングパラメータの棚卸し</h3><p>キャンペーンのリンクを貼り付けてパラメータ表を見よう。たいてい <code>utm_source</code>、<code>utm_medium</code>、<code>utm_campaign</code> に加えて <code>gclid</code>、<code>fbclid</code>、<code>msclkid</code> といったプラットフォームIDが見つかる。これらは広告プラットフォームが付加したもので正規ページからは見えないが、canonicalタグがなければ検索エンジンは別々のURLとして扱う。全体を一度に見渡すのが、解析設定でどれを除去するかを決める最短ルートだ。</p><h3>canonicalとリダイレクトの修正</h3><p>クローラーが報告したURLの解析済みオリジンとパスを、意図したcanonicalと比較する。不一致はほぼ必ず次の4つのどれかに起因する。httpとhttpsのスキーム差、wwwとapexのホスト差、末尾スラッシュの有無、大文字の混じったセグメントである。同じページが表示されても、クローラーにとっては4つとも別のURLだ。</p><h3>サーバーに何が見えているかの確認</h3><p>ハッシュ以降のフラグメントはブラウザから出て行かない。「#step=3 付きのリンクが動かない」という問い合わせがあっても、サーバーログにはパスとクエリしか残らない。ブラウザがリクエスト送信前にハッシュ以降をすべて取り除くからだ。バックエンドが反応しなければならない情報はパスかクエリ文字列に置くべきである。</p><h2>知っておきたい限界</h2><p>このツールはブラウザと同じWHATWGパーサーを使うため、結果はJavaScriptから見える値と一致する。ホスト名が小文字化される、国際化ドメインがPunycodeに変換される、既定ポートがオリジンから落とされるといった癖まで同じだ。URLを取得しに行くわけではないので、ページが存在するか、リダイレクトするか、証明書が有効かは判定できない。非常に長いクエリ文字列もここでは問題なく解析できるが、サーバーやプロキシはリクエスト行を4〜8KBに制限するのが一般的で、きれいに解析できるURLでも転送中に拒否されることがある。スキームが無い場合はhttpsを仮定し、その旨を結果の上に表示する——ドメインだけを貼り付けるときには便利だが、その仮定をサイトがTLSに対応している証拠と読み違えてはいけない。</p>",
"ko": "<h2>URL 하나를 낱낱이 분해하기</h2><p>입력란의 예시를 보자: <code>https://shop.example.com:8080/products/shoes?color=red&amp;size=10&amp;color=blue#reviews</code>. 브라우저는 이것을 하나의 긴 문자열로 보지 않는다. 일곱 개의 필드로 보며, 그 하나하나가 요청의 동작을 바꾼다.</p><p>스킴은 <code>https:</code>다 — 콜론까지 포함되는데, URL 표준이 콜론을 프로토콜 속성의 일부로 취급하기 때문이다. 호스트는 <code>shop.example.com</code>, 포트는 <code>8080</code>이며 HTTPS 기본값인 443이 아니기 때문에 명시되어 있다. 스킴과 함께 이 셋이 오리진 <code>https://shop.example.com:8080</code>을 이루고, 오리진은 브라우저가 동일 출처 정책, 호스트 단위 쿠키, CORS 판단에 사용하는 기본 단위다. 같은 컴퓨터라도 포트만 바뀌면 다른 오리진이다.</p><p>경로는 <code>/products/shoes</code>이며 <code>products</code>와 <code>shoes</code> 두 개의 세그먼트로 나뉜다. 라우터가 세그먼트를 위치 기준으로 매칭하기 때문에 세그먼트는 중요하다 — 끝에 세그먼트가 하나 더 붙거나 슬래시가 두 번 들어가면 오타와 구별되지 않는 404가 조용히 발생한다. 이어서 쿼리 문자열은 세 쌍을 담고 있고, 프래그먼트는 <code>reviews</code>다.</p><h3>인코딩 토글이 있는 이유</h3><p>URL은 제한된 ASCII 문자만 담을 수 있고, 그중 일부는 구분자 역할을 하기 때문에 예약되어 있다. 그 밖의 모든 것은 퍼센트 인코딩으로 전달된다. 바이트를 퍼센트 기호와 16진수 두 자리로 표기하는 방식이다. 공백은 <code>%20</code>(쿼리 문자열 안에서는 더하기 기호), 앰퍼샌드는 <code>%26</code>, 물음표는 <code>%3F</code>, 값 안의 슬래시는 <code>%2F</code>가 되고, 비ASCII 문자는 먼저 UTF-8로 변환되므로 é는 <code>%C3%A9</code>, 이모지는 네 개의 퍼센트 그룹이 된다.</p><p>실질적인 결과는 이렇다. <code>?q=fish%20%26%20chips</code>는 값이 \"fish &amp; chips\"인 파라미터 하나지만, <code>?q=fish &amp; chips</code>는 망가진 세 조각이다. 클라이언트가 실제로 보낸 것과 서버가 기록한 것을 비교할 때는 원본 값 보기를 켜고, 데이터 내용만 읽고 싶을 때는 끄면 된다. 흔한 버그는 이중 인코딩으로, 이미 <code>%20</code>을 포함한 값을 한 번 더 인코딩해 <code>%2520</code>이 나타나는 경우다. 디코딩 보기에서 공백 대신 <code>%20</code>으로 보이는 것이 그 신호다.</p><h3>중복 파라미터라는 함정</h3><p>예시 URL에는 <code>color</code>가 두 번 들어 있다. 이 파서는 둘을 합치지 않고 순서대로 두 행을 모두 보여준다. 올바른 답이 실제로 받는 쪽 스택에 따라 달라지기 때문이다. PHP는 키가 <code>color[]</code>로 쓰이지 않는 한 마지막 값을 유지한다. Express와 Rails는 배열로 넘겨준다. ASP.NET은 쉼표로 이어 붙여 \"red,blue\" 문자열을 만든다. 자바 서블릿은 getParameterValues를 제공하지만 getParameter는 첫 값만 반환한다. 그래서 스테이징에서 잘 되던 링크가 운영에서는 엉뚱한 색을 고를 수 있다. URL을 프로그램으로 생성한다면 쿼리 문자열을 만든 뒤가 아니라 만들기 전에 중복을 제거하라.</p><h2>일상적인 디버깅 작업 흐름</h2><h3>트래킹 파라미터 점검</h3><p>캠페인 링크를 붙여 넣고 파라미터 표를 보라. 보통 <code>utm_source</code>, <code>utm_medium</code>, <code>utm_campaign</code>과 함께 <code>gclid</code>, <code>fbclid</code>, <code>msclkid</code> 같은 플랫폼 ID가 보인다. 광고 플랫폼이 덧붙인 것으로 정식 페이지에는 보이지 않지만, 캐노니컬 태그가 따로 없으면 검색엔진은 이를 별개의 URL로 인식한다. 전체 목록을 한눈에 보는 것이 분석 설정에서 무엇을 제거할지 정하는 가장 빠른 방법이다.</p><h3>캐노니컬과 리다이렉트 바로잡기</h3><p>크롤러가 보고한 URL의 파싱된 오리진과 경로를 의도한 캐노니컬과 비교하라. 불일치는 거의 언제나 네 가지 중 하나에서 온다. http와 https 스킴 차이, www와 apex 호스트 차이, 끝 슬래시 유무, 대문자가 섞인 세그먼트다. 같은 페이지를 보여주더라도 크롤러에게는 넷 다 서로 다른 URL이다.</p><h3>서버가 볼 수 있는 범위 확인</h3><p>해시 뒤의 프래그먼트는 브라우저를 절대 벗어나지 않는다. \"#step=3이 붙은 링크가 동작하지 않는다\"는 문의가 들어와도 서버 로그에는 경로와 쿼리만 남는다. 브라우저가 요청을 보내기 전에 해시부터 뒤쪽을 모두 잘라내기 때문이다. 백엔드가 반응해야 하는 값은 경로나 쿼리 문자열에 넣어야 한다.</p><h2>알아둘 만한 한계</h2><p>이 도구는 브라우저가 쓰는 것과 같은 WHATWG 파서를 사용하므로 결과가 자바스크립트에서 보게 될 값과 일치한다. 호스트명이 소문자로 바뀌거나 국제화 도메인이 퓨니코드로 변환되거나 기본 포트가 오리진에서 빠지는 등의 특성까지 그대로다. URL을 실제로 요청하지는 않으므로 페이지가 존재하는지, 리다이렉트되는지, 인증서가 유효한지는 알려주지 못한다. 아주 긴 쿼리 문자열도 여기서는 문제없이 파싱되지만, 서버와 프록시는 보통 요청 라인을 4~8KB로 제한하므로 깔끔하게 파싱되는 URL이라도 전송 중에 거부될 수 있다. 스킴이 없으면 도구가 https를 가정하고 결과 위에 그 사실을 표시한다 — 도메인만 붙여 넣을 때 편리하지만, 그 가정을 해당 사이트가 TLS를 지원한다는 뜻으로 읽어서는 안 된다.</p>"
};
